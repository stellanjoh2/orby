/** Desktop Turntable "NORMAL" speed (rad/s) — see `index.html` autorotate value 0.2. */
export const MESH_AUTO_ROTATE_SPEED_NORMAL = 0.2;

/** Desktop Turntable "FAST" speed (rad/s). */
export const MESH_AUTO_ROTATE_SPEED_FAST = 0.5;
