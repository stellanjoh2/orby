Drop source screenshots here, then run:

```bash
npm run import:shape-library-thumbs
```

Name files by shape id: `cube.png`, `cone.png`, `pipe.png`

**Framing tip:** enable Camera → Composition Guides — the centered square overlay matches the thumbnail center-crop.

Output goes to `assets/images/shape-library-{id}.png`.
